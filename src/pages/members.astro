---
import DefaultLayout from "../layouts/DefaultLayout.astro";
import MemberCard from "../components/MemberCard.astro";

const base = import.meta.env.BASE_URL;
const resolveUrl = (path: string) => {
  if (path === "/") {
    return base;
  }
  const cleanPath = path.startsWith("/") ? path.substring(1) : path;
  return `${base}${cleanPath}`;
};

// Example data; ideally this would come from a JSON or Markdown file
const team = [
  {
    name: "Dr. Marilyn Cruces",
    role: "Radioastronomer, Professor at UC Chile Max Planck Partner group leader",
    img: resolveUrl("/images/miembros/mcruces.jpg"), // Using resolveUrl()
    profile: "https://orcid.org/0000-0001-6804-6513",
  },
  {
    name: "Cristóbal Braga",
    role: "Radioastronomer, Researcher",
    img: resolveUrl("/images/miembros/cbraga.jpg"), // Using resolveUrl()
    profile: "https://orcid.org/0009-0000-6017-4211",
  },
  {
    name: "Josefina Vera-Casanova",
    role: "Radioastronomer, Researcher",
    img: resolveUrl("/images/miembros/jvera.jpg"), // Using resolveUrl()
    profile: "https://orcid.org/0009-0002-6002-0575",
  },
];
---

<DefaultLayout>
  <h2 class="text-3xl font-bold mb-6 text-center">Our Team</h2>
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
    {
      team.map((member) => (
        <MemberCard
          name={member.name}
          role={member.role}
          img={member.img}
          profile={member.profile}
        />
      ))
    }
  </div>
</DefaultLayout>
